// types/index.ts
export interface User {
    id: number;
    name: string;
    email: string;
    // Add other user fields as necessary
  }
  
  export interface LoginCredentials {
    email: string;
    password: string;
  }
  